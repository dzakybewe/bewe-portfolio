---
interface Props {
  /** Homepage uses transparent-over-hero nav with in-page anchors.
      Inner pages force the solid "scrolled" look and link back to /#section. */
  home?: boolean;
}

const { home = true } = Astro.props;

const links = [
  { label: 'About', id: 'about' },
  { label: 'Experience', id: 'experience' },
  { label: 'Projects', id: 'projects' },
  { label: 'Stack', id: 'stack' },
  { label: 'Writing', id: 'writing' },
  { label: 'Contact', id: 'contact' },
];

const hrefFor = (id: string) => (home ? `#${id}` : `/#${id}`);
---

<nav class:list={['site-nav', { 'scrolled solid': !home }]} aria-label="Primary">
  <div class="max-w-container mx-auto px-7 lg:px-8 flex items-center justify-between h-full">
    <!-- TODO: Replace text logo with <img src="/logo.svg" alt="Dzaky Ahmadin" /> when logo asset is ready. Place logo file at public/logo.svg -->
    <a href={home ? '#top' : '/'} class="nav-logo font-serif text-[16px] md:text-[16px] leading-none">
      Dzaky Ahmadin
    </a>

    <!-- Desktop links -->
    <ul class="hidden md:flex items-center gap-7">
      {
        links.map((link) => (
          <li>
            <a
              href={hrefFor(link.id)}
              class="nav-link font-sans text-[13px] uppercase tracking-[2px]"
            >
              {link.label}
            </a>
          </li>
        ))
      }
    </ul>

    <!-- Mobile toggle -->
    <button
      id="nav-toggle"
      class="md:hidden flex flex-col justify-center gap-1.25 w-7 h-7"
      aria-label="Toggle menu"
      aria-expanded="false"
      aria-controls="nav-mobile"
    >
      <span class="nav-bar block h-[1.5px] w-6 transition-all"></span>
      <span class="nav-bar block h-[1.5px] w-6 transition-all"></span>
    </button>
  </div>

  <!-- Mobile menu -->
  <div id="nav-mobile" class="md:hidden hidden border-t border-rule bg-cream/95 backdrop-blur">
    <ul class="max-w-container mx-auto px-7 py-4 flex flex-col gap-4">
      {
        links.map((link) => (
          <li>
            <a
              href={hrefFor(link.id)}
              class="nav-link block font-sans text-[13px] uppercase tracking-[2px]"
            >
              {link.label}
            </a>
          </li>
        ))
      }
    </ul>
  </div>
</nav>

<style>
  .site-nav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    height: 64px;
    background: transparent;
    border-bottom: 0.5px solid transparent;
    transition: background 0.3s ease, border-color 0.3s ease, backdrop-filter 0.3s ease;
  }
  .site-nav.scrolled {
    background: rgba(253, 251, 246, 0.88);
    backdrop-filter: blur(8px);
    border-bottom: 0.5px solid var(--color-rule);
  }
  /* When the mobile menu is open the bar needs a solid backdrop too. */
  .site-nav.menu-open {
    background: rgba(253, 251, 246, 0.95);
    backdrop-filter: blur(8px);
  }

  .nav-link {
    position: relative;
    padding-bottom: 2px;
    border-bottom: 0.5px solid transparent;
    transition: color 0.2s ease, border-color 0.2s ease;
  }

  /* Default = nav is transparent over the dark hero → light text/marks */
  .nav-logo {
    color: var(--color-cream);
    transition: color 0.3s ease;
  }
  .nav-bar {
    background: var(--color-cream);
    transition: background 0.3s ease;
  }
  .site-nav .nav-link {
    color: var(--color-dark-text);
  }

  /* Light-background states (scrolled, or mobile menu open) → dark text/marks */
  .site-nav.scrolled .nav-logo,
  .site-nav.menu-open .nav-logo {
    color: var(--color-ink);
  }
  .site-nav.scrolled .nav-bar,
  .site-nav.menu-open .nav-bar {
    background: var(--color-ink);
  }
  .site-nav.scrolled .nav-link,
  .site-nav.menu-open .nav-link {
    color: var(--color-ink-light);
  }

  /* Mobile dropdown always sits on a light panel */
  #nav-mobile .nav-link {
    color: var(--color-ink-light);
  }

  /* Hover / active win in every state (kept last) */
  .site-nav .nav-link:hover,
  #nav-mobile .nav-link:hover {
    color: var(--color-accent);
  }
  .site-nav .nav-link.active,
  #nav-mobile .nav-link.active {
    color: var(--color-accent);
    border-bottom: 0.5px solid var(--color-accent);
  }
</style>

<script>
  const toggle = document.getElementById('nav-toggle');
  const menu = document.getElementById('nav-mobile');
  const nav = document.querySelector('.site-nav');

  function closeMenu() {
    menu?.classList.add('hidden');
    nav?.classList.remove('menu-open');
    toggle?.setAttribute('aria-expanded', 'false');
  }

  toggle?.addEventListener('click', () => {
    const isHidden = menu?.classList.toggle('hidden');
    nav?.classList.toggle('menu-open', !isHidden);
    toggle.setAttribute('aria-expanded', String(!isHidden));
  });

  // Close after tapping a link
  menu?.querySelectorAll('a').forEach((a) => a.addEventListener('click', closeMenu));
</script>
